# Sms-Email-Spam-Classifier
A machine learning–based spam classifier that detects whether a message is spam or not using the Naive Bayes algorithm. The model is trained on text data after preprocessing and feature extraction using techniques like tokenization and vectorization.
